<nav class="uk-navbar-container" uk-navbar>
  <div class="uk-navbar-left">
    <ul class="uk-navbar-nav">
      <li {{ app.request.attributes.get('_route') == 'homepage' ? 'class="uk-active"' }}><a href="{{ path('homepage') }}">ACCUEIL</a></li>
      <li {{ app.request.attributes.get('_route') == 'presentation' ? 'class="uk-active"' }}><a href="{{ path('presentation') }}">PRESENTATION</a></li>
      <li {{ app.request.attributes.get('_route') == 'product_list' ? 'class="uk-active"' }}><a href="{{ path('product_list') }}">PRODUITS</a></li>
      <li {{ app.request.attributes.get('_route') == 'contact' ? 'class="uk-active"' }}><a href="{{ path('contact') }}">CONTACT</a></li>
      {% if (app.user) %}
      <li>
        <a href="{{ path('app_logout') }}" class="uk-button uk-button-primary" style="color: #FFF;">
          Déconnexion ({{ app.user.username }})
        </a>
      </li>
      {% endif %}
    </ul>
  </div>
</nav>